package org.cancermodels.persistance;

import org.springframework.data.jpa.repository.JpaRepository;

public interface OntologyLoadReportRepository extends JpaRepository<OntologyLoadReport, Long> {
}
